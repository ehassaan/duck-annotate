
// import { SentenceSplitter } from "llamaindex";


// const splitter = new SentenceSplitter({
//     chunkSize: 1000,
// });

// export async function createChunks(args: { source_id: string, description: string, title: string, source_type: string; }) {
//     return splitter.splitText(args.description).map((chunk) => ({
//         source_id: args.source_id,
//         source_type: args.source_type,
//         title: args.title,
//         description: chunk,
//     }));
// }
